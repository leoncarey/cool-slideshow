import React from "react";
import Typography from "@material-ui/core/Typography";
import style from './style.less';

const Warns = () => {
	return (
		<div className={style.warns}>
			<Typography paragraph>
				Warns works!
			</Typography>
		</div>
	);
};

export default Warns;